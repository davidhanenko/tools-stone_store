import styled from 'styled-components';

const ItemsByCategoryStyles = styled.div`

  margin-top: calc(var(--navHeight) + var(--searchHeight));
  margin-bottom: 10rem;
  position: relative;

`;

export { ItemsByCategoryStyles };
