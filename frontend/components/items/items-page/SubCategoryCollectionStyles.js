import styled from 'styled-components';

const SubCategoryCollectionStyles = styled.article`
  padding: 2rem;
  .collection-title {
    text-transform: uppercase;
  }
  .collection {
    display:flex;
    flex-wrap: wrap;
  }
`;
export { SubCategoryCollectionStyles };
