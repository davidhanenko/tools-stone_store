{
  "routes": [
    {
      "method": "GET",
      "path": "/single-products",
      "handler": "single-product.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/single-products/count",
      "handler": "single-product.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/single-products/:id",
      "handler": "single-product.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/single-products",
      "handler": "single-product.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/single-products/:id",
      "handler": "single-product.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/single-products/:id",
      "handler": "single-product.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
